var config = require('./package.json');

module.exports = {
  entry: './lib/index.js',

  output: {
    path: './dist/',
    filename: config.name + '.min.js',
    library: config.name,
    libraryTarget: 'umd'
  },

  module: {
    loaders: [
      { test: /\.js$/, loader: 'jsx-loader?harmony' }
    ]
  }
};
